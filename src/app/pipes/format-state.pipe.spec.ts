import { FormatStatePipe } from './format-state.pipe';

describe('FormatStatePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatStatePipe();
    expect(pipe).toBeTruthy();
  });
});
